"""The Node Database layer — [MS-PST] 2.2: the header, the B-trees, the blocks.

Everything here reads Unicode stores only (ADR-0003). Modules land in the
order docs/PORTING-PLAN.md gives, and each is diffed against the oracle
goldens before the next begins.
"""
