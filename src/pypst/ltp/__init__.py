"""Lists, Tables and Properties — the layer between raw node data and MAPI.

`prop_type` (P22) is the leaf: the property type codes and one pure decoder
per type. `heap`, `tree`, `prop_context` and `table_context` build on it.
"""
